## Make a file named credentials.js in the server folder
```JS
module.exports = {
    'Twitter' : {
	    consumer_key: 'x',
	    consumer_secret: 'x',
	    access_token: 'x',
	    access_token_secret: 'x'
    }
}
```
